apply plugin: 'kotlin'

compileKotlin {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8

    kotlinOptions {
        jvmTarget = "1.8"
        moduleName = "com.task.noteapp.commons"
    }
}

dependencies {
    implementation libs.javax.inject
}